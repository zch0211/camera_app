package com.camera.app.collection.entity;

public enum ProbeType {
    PORT_SCAN,
    HTTP_TITLE,
    BANNER,
    RTSP_PROBE,
    ONVIF_PROBE,
    WEB_FINGERPRINT,
    SNMP_PROBE,
    SSH_BANNER,
    TELNET_BANNER,
    UPNP_PROBE
}
